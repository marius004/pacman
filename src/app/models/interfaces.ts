export interface Position {
    x: number;
    y: number;
}

export interface Direction {
    x: number;
    y: number;
}

export interface Dot {
    gridX: number;
    gridY: number;
    type: number;
}